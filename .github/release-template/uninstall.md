## Uninstall

Run `vrcresolver.Uninstaller.exe` from the install folder. No prompt; runs to completion. Restores the original `yt-dlp.exe`, removes the hosts entry, wipes `%LOCALAPPDATA%Low\vrcresolver\`, deletes the install directory.
