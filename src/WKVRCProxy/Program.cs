// Wired up in commit (f).
namespace WKVRCProxy;

internal static class Program
{
    private static int Main(string[] args)
    {
        Console.WriteLine("WKVRCProxy stub — wiring lands in commit (f).");
        return 0;
    }
}
