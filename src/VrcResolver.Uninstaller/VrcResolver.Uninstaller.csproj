<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <RootNamespace>VrcResolver.Uninstaller</RootNamespace>
    <AssemblyName>vrcresolver.Uninstaller</AssemblyName>
    <Company>RealWhyKnot</Company>
    <Product>VRCResolver</Product>
    <AssemblyTitle>VRCResolver uninstaller</AssemblyTitle>
    <!-- AOT publish so the uninstaller is ~10x smaller. Trim-safe:
         pure file/process operations + Logger/CrashHandler/WkvrcPaths/
         ToolsDirSweeper from Shared. No JSON serialization. No reflection
         over user types. No JIT-compiled regex (ToolsDirSweeper converted
         to [GeneratedRegex]).
         InvariantGlobalization drops ICU (~1 MB). -->
    <PublishAot>true</PublishAot>
    <PublishTrimmed>true</PublishTrimmed>
    <TrimMode>full</TrimMode>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\VrcResolver.Shared\VrcResolver.Shared.csproj" />
  </ItemGroup>

</Project>
